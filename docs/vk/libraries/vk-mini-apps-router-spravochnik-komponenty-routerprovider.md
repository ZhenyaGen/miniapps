# RouterProvider

**Раздел:** Библиотеки → vk-mini-apps-router → Справочник → Компоненты → RouterProvider  
**Источник:** документация VK для разработчиков (dev.vk.com)

---

`RouterProvider` — компонент, который предоставляет объект [роутера](https://dev.vk.ru/ru/libraries/router) и дополнительные параметры для вашего React-приложения. Позволяет использовать [хуки библиотеки vk-mini-apps-](https://dev.vk.ru/ru/libraries/router/reference#Hooks) [router](https://dev.vk.ru/ru/libraries/router/reference#Hooks) во вложенных компонентах.

Используйте `RouterProvider` как компонент-обёртку для компонента вашего приложения.

## Пример

```ts
import { AdaptivityProvider, AppRoot, ConfigProvider } from '@vkontakte/vkui';
import App from './App'; // Компонент вашего приложения
import { router } from './routes';
import { RouterProvider } from 'dist/index';
```

```
export const AppConfig = () => {
  return (
    <ConfigProvider>
      <AdaptivityProvider>
        <AppRoot>
          <RouterProvider router={router} notFound={<p>'Custom not
found'</p>}>
            <App />
          </RouterProvider>
        </AppRoot>
      </AdaptivityProvider>
    </ConfigProvider>
  );
}
```

## Свойства

| Свойство | Тип | Описание |
|---|---|---|
| `router` обязательное | [`Router`](https://dev.vk.ru/ru/libraries/router/reference/functions/createHashRouter#%D0%A0%D0%B5%D0%B7%D1%83%D0%BB%D1%8C%D1%82%D0%B0%D1%82) | Объект типа `Router`, созданный при помощи функции [`createHashRouter`](https://dev.vk.ru/ru/libraries/router/reference/functions/createHashRouter) [.](https://dev.vk.ru/ru/libraries/router/reference/functions/createHashRouter) |
| `throttled` необязательное | `boolean` | Включение задержки для навигации. |

Если значение `true`, то частота возникновения событий навигации ограничена параметром

`interval`. Если в течение этого периода произойдёт несколько событий навигации, будет обработано

| Свойство | Тип | Описание |
|---|---|---|
| только последнее. Такая функциональность помогает | обеспечить работоспособность и плавность анимаций | VKUI. |

Если свойство установлено в `false`, то специальная обработка событий навигации не используется.

Значение по умолчанию: `true`.

| `interval` необязательное | `number` Минимальная задержка между двумя последовательными навигациями в миллисекундах. Используется, когда `throttled: true`. |
|---|---|

Значение по умолчанию: `400`.

| `useBridge` необязательное | `boolean` Указывает, использует React-приложение библиотеку [VK Bridge](https://dev.vk.ru/ru/bridge/overview) или нет. |
|---|---|

Если React-приложение — это [мини-приложение](https://dev.vk.ru/ru/mini-apps/overview), которое запускается из десктопной или мобильной версии сайта ВКонтакте или из мобильного приложения ВКонтакте для Android или iOS, свойство должно быть `true`.

В ином случае свойство должно быть `false`.

Значение по умолчанию: `true`.

| `notFound` необязательное | `JSX.Element` React-компонент, который будет отображён при попытке перехода по несуществующему URL, то есть при возникновении ошибки `404 Not Found`. |
|---|---|

Атрибут игнорируется, если указан атрибут

`notFoundRedirectPath` или определён маршрут со свойством `path: '*'`. Подробности — в разделе [Обработка ошибок](https://dev.vk.ru/ru/libraries/router/handling-errors).

| `notFoundRedirectPath` необязательное | `string` Адрес страницы в приложении, например |
|---|---|

`/persik/welcome-screen`, который будет открыт при попытке перехода по несуществующему URL, то есть при возникновении ошибки `404 Not Found`. Подробности — в разделе [Обработка ошибок](https://dev.vk.ru/ru/libraries/router/handling-errors).

| Свойство | Тип | Описание |
|---|---|---|
| `hierarchy` необязательное | `RouteLeaf[]` | Древовидная структура адресов, которые будут добавлены в историю переходов, когда пользователь входит в приложение по ссылке, содержащей параметр `?inflate=true`. Используйте эту структуру, чтобы наполнить историю переходов для каждого маршрута в вашем приложении. Примеры работы с этим свойством даны ниже. |

Значение по умолчанию: `undefined`.

Компонент `RouterProvider` также содержит стандартные свойства, которые используются в React-компонентах, например `children`, которое предоставляет доступ к вложенным JSX- элементам.

## Использование свойства `hierarchy`

Используйте свойство `hierarchy`, чтобы вставить дополнительные пути в историю навигации. Роутер будет добавлять их каждый раз, когда пользователь переходит в приложение по ссылке с query-параметром `?inflate=true`. Давайте рассмотрим, зачем это может быть нужно.

Предположим, в вашем приложении есть главная страница, каталог товаров и карточка отдельного товара. При запуске приложения пользователь обычно проходит последовательно через главную страницу в каталог и далее переходит к карточке товара. В историю переходов попадают адреса всех этих страниц.

Если же пользователь переходит в приложение по прямой ссылке на карточку товара, то в историю переходов попадёт только адрес карточки. И если пользователь нажмёт на кнопку «Назад», он выйдет из приложения.

Такой выход может быть нежелательным. С помощью свойства `hierarchy` вы можете добавить в историю переходов дополнительные страницы, например каталог товаров и главную, тогда нажатии «Назад» пользователь останется в приложении.

Свойство `hierarchy` содержит массив объектов `RouteLeaf`, которые описывают пути для добавления в историю.

### Объявление объекта RouteLeaf

```ts
export interface RouteLeaf {
  path: string;
  children?: RouteLeaf[];
}
```

| Свойство | Тип | Описание |
|---|---|---|
| `path` обязательное | `string` | Путь в приложении. Может содержать [параметры](https://dev.vk.ru/ru/libraries/router/parameters), например |

`/catalog/:id`.

**Свойство | Тип | Описание**

Важно. Значения этого свойства должны соответствовать маршрутам, которые вы определяете в своём приложении.

| `children` необязательное | `RouteLeaf[]` [Список путей, которые следуют за текущим путём](https://dev.vk.ru/ru/libraries/router/setting-routes) [`path`](https://dev.vk.ru/ru/libraries/router/setting-routes) [в](https://dev.vk.ru/ru/libraries/router/setting-routes) истории. |
|---|---|

### Пример использования

```ts
import { AdaptivityProvider, AppRoot, ConfigProvider } from '@vkontakte/vkui';
import App from './App'; // Компонент вашего приложения
import { router } from './routes';
import { RouterProvider } from 'dist/index';
```

```
export const hierarchy: RouteLeaf[] = [{
  path: '/',
  children: [
    {
      path: `/catalog}`,
      children: [
        { path: `/catalog/:id`,
          children: [
            { path: `/catalog/:id/modal_page`}
          ]
        }
      ],
    }
  ],
}];
```

```
export const AppConfig = () => {
  return (
    <ConfigProvider>
      <AdaptivityProvider>
        <AppRoot>
          <RouterProvider router={router} hierarchy={hierarchy}>
            <App />
          </RouterProvider>
        </AppRoot>
      </AdaptivityProvider>
    </ConfigProvider>
  );
}
```

Дерево описывает возможные пути входа в приложение. Приложению можно передавать ссылки, которые соответствуют как концевым узлам дерева, так и средним.

Например, если передать такому приложению ссылку `/catalog/123/modal_page?` `inflate=true`, в историю переходов будут записаны следующие страницы:

- `/catalog/123/modal_page` (переданный путь)
- `/catalog/123` (предыдущий по отношению к переданному пути)
- `/catalog`
- `/` (самый ранний по отношению к переданному пути)

Если приложению передать ссылку из середины дерева, например `/catalog/123?inflate=true`, в историю переходов будут записаны следующие страницы:

- `/catalog/123` (переданный путь)
- `/catalog` (предыдущий по отношению к переданному пути)
- `/` (самый ранний по отношению к переданному пути)

### Замечания

Любой элемент дерева `hiearchy` — это маршрут, который может быть использован в прямых ссылках. Подумайте о возможных путях входа в ваше приложение, о дальнейших перемещениях с таких страниц и создайте элементы для каждой возможной прямой ссылки.

Если элемент дерева `hierarhy` встречается в нескольких ветках, приоритетной историей переходов будет считаться та, где этот элемент является оконечным узлом.

## Материалы по теме

- [Установка и подключение](https://dev.vk.ru/ru/libraries/router/setup)
- [Справочник vk-mini-apps-router](https://dev.vk.ru/ru/libraries/router/reference)
