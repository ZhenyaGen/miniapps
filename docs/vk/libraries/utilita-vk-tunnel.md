# Утилита VK Tunnel

**Раздел:** Библиотеки → Утилита VK Tunnel  
**Источник:** документация VK для разработчиков (dev.vk.com)

---

## VK Tunnel

#### Из-за технических работ с 2 октября 2025 года VK Tunnel недоступен

При подключении сервис вернёт ошибку. Для отладки и демонстрации ваших веб-приложений рекомендуем временно использовать сторонние решения.

VK Tunnel — npm-пакет, с помощью которого вы можете сделать локальный веб-сервер доступным в интернете. Это нужно при отладке и демонстрации веб-приложений.

Файлы создаваемого веб-приложения находятся на компьютере разработчика. Чтобы проверить взаимодействие приложения с внешними сервисами, например с платформой ВКонтакте, или продемонстрировать работу приложения, необходимо сделать локальный веб-сервер доступным в интернете. VK Tunnel помогает выполнить эту задачу.

VK Tunnel запускается на компьютере разработчика и создаёт специальное соединение (туннель), с помощью которого локальный веб-сервер становится доступным в интернете. Во время работы туннеля все внешние запросы к серверу и ответы на них проходят через туннель.

Альтернативой использованию VK Tunnel является загрузка файлов веб-приложения на какой- нибудь публично доступный сервер или предоставление доступа к локальному серверу другим способом.

VK Tunnel предоставляет доступ к локальному серверу по протоколам HTTP, HTTPS, WebSocket и WebSocket Secure.

## Исходный код

GitHub: [https://github.com/vkcom/vk-tunnel-client](https://github.com/vkcom/vk-tunnel-client)

## Установка

#### yarn

```
yarn global add @vkontakte/vk-tunnel
```

#### npm

```
npm install @vkontakte/vk-tunnel -g
```

Мы рекомендуем указать `global` для yarn или `-g` для npm, чтобы устанавливаемый пакет был доступен всем проектам на вашем компьютере.

## Запуск и использование

### 1. Укажите настройки

Перед запуском VK Tunnel вам может потребоваться создать переменные окружения или конфигурационный файл с настройками утилиты. Настройки можно указать и при запуске VK Tunnel из командной строки. Подробнее о настройках — ниже.

### 2. Запустите локальный веб-сервер

Для старта VK Tunnel необходимо, чтобы ваш веб-сервер был запущен. Если он ещё не работает, запустите его.

### 3. Запустите VK Tunnel

#### В приложениях, созданных из шаблона

В мини-приложениях, созданных с помощью [пакета](https://dev.vk.ru/ru/mini-apps/getting-started/create-vk-mini-app) [`create-vk-mini-app`](https://dev.vk.ru/ru/mini-apps/getting-started/create-vk-mini-app), VK Tunnel подключён и сконфигурирован, а в файл `package.json` добавлен скрипт для его запуска.

Чтобы запустить VK Tunnel в таких приложениях, выполните следующую команду:

#### Командная строка

```
npm run tunnel
```

#### В произвольном приложении

Чтобы запустить VK Tunnel для любого серверного приложения, в командной строке укажите `vk-` `tunnel` и параметры запуска, например так:

#### Командная строка

```
vk-tunnel --insecure=1 --http-protocol=http --ws-protocol=ws --host=localhost
--port=5173 --timeout=5000
```

### 4. Получите адрес для локального сервера

1. После запуска VK Tunnel покажет URL вида `https://oauth.vk.ru/code_auth?` `stage=check&code=2f...cba`. Откройте этот URL в браузере, чтобы пройти авторизацию.

2. Вернитесь в окно командной строки и подтвердите действие: введите Y/y или нажмите Enter.

VK Tunnel создаст специальное соединение и отобразит HTTP- и WebSocket-адреса, которые вы можете использовать для доступа к локальному серверу из интернета.

Скопируйте HTTP-адрес и укажите его в настройках вашего мини-приложения или игры.

## Настройки

Указать настройки утилиты VK Tunnel вы можете с помощью:

- Переменных окружения («env» в таблице ниже).
- Параметров командной строки («>…» в таблице ниже).
- Значений в конфигурационном файле `vk-tunnel-config.json` («json» в таблице ниже).

### Список настроек

| Настройка | Описание |
|---|---|
| env >… json | `PROXY_HOST` `--host` `host` | Адрес компьютера, на котором запущен локальный сервер. Значение по умолчанию: `localhost`. |

| Настройка | Описание |
|---|---|
| env >… json | `PROXY_PORT` `--port` `port` | Порт, на котором запущен локальный сервер. Значение по умолчанию: `5173`. |
| env >… json | `PROXY_HTTP_PROTO` `--http-protocol` `http-protocol` | Схема для протокола HTTP. Возможные значения: - `http` — нешифрованное соединение (используется по умолчанию). - `https` — шифрованное соединение. |
| env >… json | `PROXY_WS_PROTO` `--ws-protocol` `wss-protocol` | Схема для WebSocket-протокола. Возможные значения: - `ws` — нешифрованное соединение (используется по умолчанию). - `wss` — шифрованное соединение. |
| env >… json | `PROXY_WS_ORIGIN` `--ws-origin` `ws-origin` | Определяет, использовать ли для WebSocket- соединений заголовок `Origin` такой же, как и у проксируемого сервера. Возможные значения: - `1` — использовать заголовок `Origin` проксируемого сервера. Значение по умолчанию. - `0` — не использовать заголовок `Origin`. |
| env >… json | `NODE_TLS_REJECT_UNAUTHORIZED` `--insecure` `insecure` | Флаг, который позволяет пропустить проверку самоподписанных сертификатов безопасности. Возможные значения: - `0` — пропустить проверку. - `1` — проверить сертификат. |
| env >… json | `PROXY_TIMEOUT` `--timeout` `timeout` | Максимальное время ожидания ответа от вашего приложения. Значение по умолчанию: `5000` (5 секунд). Увеличьте это значение, если мини- приложение получает ответ с ошибкой `Bad gateway` и вы видите в логах запись `Error: timeout of` `5000ms exceeded`. Максимальное значение: `15000` (15 секунд). |

### Дополнительные настройки в vk-tunnel-config.json

При отладке мини-приложения или игры было бы удобно, чтобы сгенерированный адрес автоматически подставлялся в настройки мини-приложения или игры. VK Tunnel предоставляет такую возможность. Для этого укажите следующие настройки в файле `vk-tunnel-config.json`:

| Настройка | Описание |
|---|---|
| `app_id` | Идентификатор разрабатываемого мини-приложения или игры ВКонтакте. Вы можете найти его в настройках мини-приложения или игры. |
| `endpoints` | Массив, в котором вы указываете список платформ, а также [группы тестировщиков](https://dev.vk.ru/ru/mini-apps/settings/test-groups), для которых будет использоваться сгенерированный адрес. ВКонтакте автоматически установит этот адрес в [настройках приложения](https://dev.vk.ru/ru/mini-apps/settings/general/placement) и [тестовых групп](https://dev.vk.ru/ru/mini-apps/settings/test-groups). |

**Настройка | Описание**

Первые элементы массива — одна или несколько строк, которые указывают платформы:
- `mobile` — мобильное приложение ВКонтакте.
- `mvk` — мобильная версия сайта.
- `web` — десктопная версия сайта.

Далее вы можете указать группы тестировщиков в формате

`testing_group:название-группы`. Пример использования в JSON-файле — ниже.

Тестовые группы используются только для мини-приложений. Игры их не поддерживают.

Указать можно не более трёх групп. Если указать больше, адрес не будет заменён ни для одной из групп.

### Пример JSON-файла

```json
{
    "app_id": "123456",
    "endpoints": [
        "mobile",
        "mvk",
        "web",
        "testing_group:Тестовая группа 1",
        "testing_group:Тестовая группа 2"
    ],
    "port": "3000",
    "http-protocol": "https",
    "insecure": "0"
}
```

### Приоритет настроек

Параметры командной строки имеют наивысший приоритет. Если они не указаны, VK Tunnel использует значения из конфигурационного файла `vk-tunnel-config.json`. Если какое-то значение отсутствует в этом файле, используется переменная окружения. Если же и переменная не определена, то используется значение параметра по умолчанию.

## Возможные ошибки при запуске

`Error: connect ECONNREFUSED 127.0.0.1:10888`.

Убедитесь, что вы запустили свой локальный веб-сервер.

`Error: Пользователь не является создателем приложения`

Убедитесь, что вас добавили в администраторы мини-приложения или игры:

- [Администраторы (настройки мини-приложений)](https://dev.vk.ru/ru/mini-apps/settings/managers)
- [Администраторы (настройки игр)](https://dev.vk.ru/ru/games/settings/managers)

Если вы есть в списке администраторов приложения:

1. Удалите файл, содержащий токен утилиты:

- На Windows:

`C:\Users\<папка-пользователя>\.config\configstore\@vkontakte\vk-` `tunnel.json`
- На macOS и Linux:

`~/.config/configstore/@vkontakte/vk-tunnel.json`

2. Запустите VK Tunnel заново.

## Материалы по теме

- [Мини-приложения — Первые шаги](https://dev.vk.ru/ru/mini-apps/getting-started)
