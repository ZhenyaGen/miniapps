# Клавиатура

**Раздел:** Интеграция → Чат-боты → Разработка → Клавиатура  
**Источник:** документация VK для разработчиков (dev.vk.com)

---

Клавиатура чат-бота — блок с кнопками, которые показываются на экране ввода сообщения. Они упрощают и ускоряют общение: пользователи могут отправить сообщение или запрос, не печатая текст.

Количество кнопок в блоке, их название, цвет, выполняемые действия и другие параметры можно настроить. Это даёт возможность формировать блок в соответствии со спецификой чата или предыдущих ответов пользователя.

## Как использовать

### Включите клавиатуру в настройках сообщества

Чтобы клавиатура отображалась в окне чата, включите её:

1. Откройте ваше сообщество ВКонтакте.

2. Перейдите на страницу Управление → Сообщения и включите Сообщения сообществ.

Сохраните изменения.

3. Переключитесь в раздел Управление → Сообщения → Настройки для бота и включите Возможности ботов.

Вы можете поставить галочку в пункте Добавить кнопку «Начать». В этом случае клавиатура по умолчанию будет содержать кнопку «Начать».

Сохраните изменения.

Теперь вы можете добавлять клавиатуру к сообщениям.

### Добавьте клавиатуру к сообщениям

Чтобы добавить клавиатуру к сообщению, используйте [API ВКонтакте](https://dev.vk.ru/ru/reference): отправьте ответ пользователю с помощью API-запроса [`messages.send`](https://dev.vk.ru/ru/method/messages.send) и укажите желаемую конфигурацию кнопок в параметре `keyboard` этого запроса. Параметр принимает данные в виде JSON-строки. Формат используемых объектов и их свойств описан ниже.

Если в формате будут ошибки, запрос [`messages.send`](https://dev.vk.ru/ru/method/messages.send) вернёт ошибку `911. Keyboard format` `is invalid`.

## Описание клавиатуры

### Пример

Описание клавиатуры задаётся в виде JSON-строки. Передайте её в параметр `keyboard` API- запроса [`messages.send`](https://dev.vk.ru/ru/method/messages.send) [.](https://dev.vk.ru/ru/method/messages.send)

```json
{
  "one_time": false,
  "buttons":[
    [
      {
        "action":{
          "type":"location",
          "payload":"{\"button\": \"1\"}"
        }
      }
    ],
    [
      {
        "action":{
          "type":"vkpay",
          "hash":"action=transfer-to-group&group_id=12345&aid=10"
        }
      }
    ],
    [
      {
        "action":{
          "type":"text",
          "payload":"{\"button\": \"1\"}",
          "label":"Red"
        },
        "color":"negative"
      },
      {
        "action":{
          "type":"text",
          "payload":"{\"button\": \"2\"}",
          "label":"Green"
        },
        "color":"positive"
      },
      {
        "action":{
          "type":"text",
          "payload":"{\"button\": \"2\"}",
          "label":"Blue"
        },
        "color":"primary"
      },
      {
        "action":{
          "type":"text",
          "payload":"{\"button\": \"2\"}",
          "label":"White"
        },
        "color":"secondary"
      }
    ]
  ]
}
```

### Объект, описывающий клавиатуру

| Поле | Тип | Описание |
|---|---|---|
| `buttons` обязательное | `array` | Массив, элементы которого соответствуют рядам клавиатуры. Каждый элемент, в свою очередь, тоже является массивом. Этот массив содержит объекты, описывающие кнопки. |
| `one_time` необязательное | `bool` | Указывает, надо ли скрывать клавиатуру после нажатия на какую-либо кнопку на ней ( `true`) или нет ( `false`). |

Работает, только если `"inline": false`.

| `inline` необязательное | `bool` Определяет тип клавиатуры. Подробности — ниже. |
|---|---|

### Объект, описывающий кнопку

| Поле | Тип | Описание |
|---|---|---|
| `action` обязательное | `object` | Объект, описывающий тип кнопки и её параметры. |
| `color` необязательное | `string` | Цвет кнопки. Параметр используется только для кнопок типа `text` и |

`callback`.

### Возможные значения цвета кнопок

| Константа Описание Цвет в светлой теме | Цвет в тёмной теме |
|---|---|
| `primary` Используется для кнопок, выполняющих основное действие. | `#5181B8` `#E1E3E6` |
| `secondary` | Используется для обычных кнопок. `#E5EBF1` `#424242` |
| Константа Описание Цвет в светлой теме | Цвет в тёмной теме |
| `negative` Используется для кнопок, выполняющих опасное действие или отмену, например «Удалить» или «Отклонить». | `#E64646` `#FF5C5C` |
| `positive` Используется для кнопок, с помощью которых пользователь выражает согласие или подтверждает свои действия, например «Согласиться» или «Подтвердить». | `#4BB34B` `#4BB34B` |

## Типы клавиатур

Поле `inline` в объекте, описывающем клавиатуру, указывает тип клавиатуры.

### Клавиатура чата

Если `"inline": false`, то клавиатура располагается под полем ввода текста в окне чата.

Такая клавиатура может иметь до 40 кнопок, которые могут быть сгруппированы в 10 рядов до 5 кнопок в каждом.

### Inline-клавиатура

Если `"inline": true`, клавиатура показывается в поле сообщения.

Такая клавиатура может иметь до 10 кнопок, сгруппированных в 6 рядов до 5 кнопок в каждом.

### Общие параметры клавиатур

- Высота всех кнопок одинаковая.
- Кнопки, расположенные в одной строке, имеют одинаковую ширину.
- Ширина каждой строки равна ширине клавиатуры.
- Текст отображается по центру кнопки и обрезается, если не помещается в границы кнопки полностью.

## Типы кнопок

Вы можете добавлять различные кнопки на клавиатуру. Мы называем кнопки по их типу, который указывается в поле `type`.

### text

Отправляет сообщение с текстом, который указан в поле `label`.

| Поле | Тип | Описание |
|---|---|---|
| `type` обязательное | `string` | Тип кнопки. Должен быть `"text"`. |

| Поле | Тип | Описание |
|---|---|---|
| `label` обязательное | `string` | Текст для вставки в сообщение. Максимальная длина — 40 символов. |

Этот текст также отображается как текст кнопки.

| `payload` необязательное | `string` Дополнительные данные в формате JSON-строки. Они будут отправлены серверу бота в сообщении [`message_new`](https://dev.vk.ru/ru/api/community-events/json-schema#message_new), в поле |
|---|---|

`payload`. Максимальная длина — 255 символов.

Строка должна представлять собой описание JSON-объекта, то есть начинаться и заканчиваться фигурными скобками.

Для `text` -кнопок вы также можете указать цвет кнопки.

### location

Открывает диалоговое окно с информацией о местоположении. Пользователь может выбрать своё местоположение в этом окне и отправить его в чат. Администраторы сообщества получат изображение карты в чате. Координаты точки придут на сервер бота в поле

`message.geo.coordinates` в сообщении [`message_new`](https://dev.vk.ru/ru/api/community-events/json-schema#message_new) [.](https://dev.vk.ru/ru/api/community-events/json-schema#message_new)

Ряд, в котором находится кнопка `location`, может содержать не больше 2 кнопок. В противном случае при вызове `messages.send` произойдёт ошибка.

| Поле | Тип | Описание |
|---|---|---|
| `type` обязательное | `string` | Тип кнопки. Должен быть `"location"`. |
| `payload` необязательное | `string` | Дополнительные данные в формате JSON-строки. Они будут отправлены серверу бота в сообщении [`message_new`](https://dev.vk.ru/ru/api/community-events/json-schema#message_new), в поле |

`payload`. Максимальная длина — 255 символов.

Строка должна представлять собой описание JSON-объекта, то есть начинаться и заканчиваться фигурными скобками.

vkpay

Открывает окно оплаты VK Pay с предопределёнными параметрами. Кнопка называется «Оплатить через VK Pay», VK Pay отображается как логотип.

Кнопка должна быть единственной в строке. В противном случае при вызове `messages.send` произойдёт ошибка.

| Поле | Тип | Описание |
|---|---|---|
| `type` обязательное | `string` | Тип кнопки. Должен быть `"vkpay"`. |
| `hash` обязательное | `string` | Строка, содержащая [параметры платежа VK Pay](https://dev.vk.ru/ru/pay/getting-started) и идентификатор приложения в параметре `aid`, отделённый `&`. Пример: |

`action=transfer-to-group&group_id=1&aid=10`.

| `payload` необязательное | `string` Дополнительные данные в формате JSON-строки. Они будут отправлены серверу бота в сообщении [`message_new`](https://dev.vk.ru/ru/api/community-events/json-schema#message_new), в поле |
|---|---|

`payload`. Максимальная длина — 255 символов.

Строка должна представлять собой описание JSON-объекта, то есть начинаться и заканчиваться фигурными скобками.

### open_link

Открывает ссылку, указанную в поле `link`.

| Поле | Тип | Описание |
|---|---|---|
| `type` обязательное | `string` | Тип кнопки. Должен быть `"open_link"`. |
| `link` обязательное | `string` | Ссылка, которая будет открыта при нажатии на кнопку. |
| `label` обязательное | `string` | Текст кнопки. |
| `payload` необязательное | `string` | Дополнительные данные в формате JSON-строки. Они будут отправлены серверу бота в сообщении [`message_new`](https://dev.vk.ru/ru/api/community-events/json-schema#message_new), в поле |

`payload`. Максимальная длина — 255 символов.

Строка должна представлять собой описание JSON-объекта, то есть начинаться и заканчиваться фигурными скобками.

open_app

Открывает мини-приложение или игру, идентификатор которой указан в параметре `app_id`.

Мини-приложение, добавленное в сообщество, может передавать данные боту через Callback или LongPoll API. Подробности — в разделе Общение бота с приложением.

| Поле | Тип | Описание |
|---|---|---|
| `type` обязательное | `string` | Тип кнопки. Должен быть `open_app`. |

| Поле | Тип | Описание |
|---|---|---|
| `app_id` обязательное | `integer` | Идентификатор мини-приложения или игры, которую надо открыть. [Где найти идентификатор приложения?](https://dev.vk.ru/ru/mini-apps/settings/overview#%D0%93%D0%B4%D0%B5%20%D0%BD%D0%B0%D0%B9%D1%82%D0%B8%20ID%20%D0%BC%D0%B8%D0%BD%D0%B8-%D0%BF%D1%80%D0%B8%D0%BB%D0%BE%D0%B6%D0%B5%D0%BD%D0%B8%D1%8F?) |
| `label` обязательное | `string` | Текст кнопки, например `"Играть"`. |
| `owner_id` необязательное | `integer` | Идентификатор сообщества, в котором установлено мини- приложение, если его требуется открыть в контексте сообщества. [Где](https://vk.com/faq18062) [найти идентификатор сообщества?](https://vk.com/faq18062) |
| `hash` необязательное | `string` | Хеш для навигации в приложении, он будет передан в строке параметров запуска после символа `#`. |
| `payload` необязательное | `string` | Дополнительные данные в формате JSON-строки. Они будут отправлены серверу бота в сообщении [`message_new`](https://dev.vk.ru/ru/api/community-events/json-schema#message_new), в поле |

`payload`. Максимальная длина — 255 символов

Строка должна представлять собой описание JSON-объекта, то есть начинаться и заканчиваться фигурными скобками.

callback

Нажатие на кнопку не отправляет какое-либо сообщение от пользователя в чат, но направляет серверу бота сообщение [`message_event`](https://dev.vk.ru/ru/api/community-events/json-schema#message_event), которое информирует о том, что пользователь нажал кнопку. В серверном коде вы можете проверить параметры сообщения и выполнить какие-то действия в ответ на нажатие. Подробности работы с кнопками — в разделе Работа с callback- кнопками.

| Поле | Тип | Описание |
|---|---|---|
| `type` обязательное | `string` | Для этой клавиши имеет значение `callback`. |
| `label` обязательное | `string` | Текст кнопки. Максимальная длина — 40 символов. |

После нажатия на кнопку текст на ней сменяется курсором, означающим ожидание.

| `payload` необязательное | `string` Дополнительные данные в JSON-формате. Они будут отправлены серверу бота в сообщении [`message_event`](https://dev.vk.ru/ru/api/community-events/json-schema#message_event), в поле `payload`. Максимальная длина — 255 символов. |
|---|---|

Если на клавиатуре несколько `callback` -кнопок, используйте это значение, чтобы идентифицировать кнопку, которую пользователь нажал.

Строка должна представлять собой описание JSON-объекта, то есть начинаться и заканчиваться фигурными скобками.

Для `callback` -кнопок вы также можете указать цвет кнопки.

## Особенности работы

- В зависимости от параметра клавиатуры `inline` в запросе `messages.send`, ВКонтакте может либо скрыть клавиатуру, после того как пользователь нажмёт какую-либо кнопку, либо продолжить использовать клавиатуру в последующих сообщениях. При дальнейшем использовании, чтобы изменить кнопки, передайте новую конфигурацию в параметре

`keyboard` при отправке сообщения.

Чтобы скрыть клавиатуру в чате, передайте в параметре `keyboard` пустой набор клавиш:

#### Cодержимое параметра keyboard, JSON

```
{
  "buttons": []
}
```

- Значения, указанные в параметре `payload` из описания кнопки, передаются на сервер чат- бота. Некоторые технически грамотные пользователи могут подменять это значение при отправке. Поэтому мы рекомендуем проверять поступающие в `payload` данные на стороне сервера и принимать к обработке только допустимые значения.

## Работа с callback-кнопками

Callback-кнопки позволяют боту получать уведомление о нажатиях на кнопки без отправки сообщения от пользователя и после этого выполнять необходимое действие, например редактировать сообщение, показывать текстовую информацию, отправлять новую клавиатуру или открывать ссылку.

### Внешний вид

Callback-кнопки выглядят так же, как и обычные кнопки, и поддерживают все стили. После нажатия на кнопку на время выполнения действия возникает индикатор загрузки.

### Примеры использования

- Реализация основного меню бота: после нажатия на callback-кнопку на клавиатуре бот обновляет её, выполняя переход на уровень глубже по вложенности меню. При этом пользователю не нужно отправлять боту дополнительное сообщение.
- Редактирование сообщения: пользователь нажимает на callback-кнопку в inline-клавиатуре из сообщения, выбирая категорию блюда в меню. Бот редактирует сообщение, предлагая блюда из этой категории. Пользователь при этом не отправляет боту сообщение.
- Отправка результата действия: пользователь нажимает на callback-кнопку, например «Пополнить баланс», которая совершает определённое действие. После его успешного выполнения бот показывает пользователю информацию «Баланс пополнен» при помощи снекбара. Отправка сообщения при этом не требуется.

### Принцип работы

- Бот отправляет клавиатуру с callback-кнопкой.
- Пользователь нажимает на кнопку.
- Бот получает новое событие `message_event` в Callback или LongPoll API.

- Бот определяет действие с помощью параметра `event_data` в методе

[`messages.sendMessageEventAnswer`](https://dev.vk.ru/ru/method/messages.sendMessageEventAnswer) [.](https://dev.vk.ru/ru/method/messages.sendMessageEventAnswer)
- Код на стороне пользователя получает ответ от бота и выполняет нужное действие.

### Типы действий

Действие `show_snackbar`

Показать исчезающее сообщение. Снекбар показывается в течение 10 секунд и автоматически скрывается, при этом у пользователя есть возможность смахнуть его с экрана.

| Поле | Тип | Описание |
|---|---|---|
| `type` | `string` | Строка `'show_snackbar'`. |
| `text` | `string` | Текст, который нужно вывести (максимум — 90 символов). |

Действие `open_link`

Осуществляется переход по указанному адресу.

| Поле | Тип | Описание |
|---|---|---|
| `type` | `string` | Строка `open_link`. |
| `link` | `string` | URL, который нужно открыть. |

Действие `open_app`

Открыть VK Mini App. Происходит переход в мини-приложение.

| Поле | Тип | Описание |
|---|---|---|
| `type` | `string` | Строка `'open_app'`. |
| `app_id` | `int` | Идентификатор вызываемого приложения с типом VK Mini Apps. |

`owner_id` `int` Идентификатор сообщества, в котором установлено приложение, если требуется открыть в контексте сообщества.

`hash` `string` Хеш для навигации в приложении. Будет передан в строке параметров запуска после символа `#`.

## Общение бота с приложением

Приложения, установленные в сообществах, могут генерировать событие `app_payload` в Callback или LongPoll API с помощью события [`VKWebAppSendPayload`](https://dev.vk.ru/ru/bridge/VKWebAppSendPayload) библиотеки [VK Bridge](https://dev.vk.ru/ru/bridge/overview).

Бот может передавать приложению параметры при запуске, используя поле `hash`.

Если необходимо наладить получение события от бота в приложении, то такого механизма не существует, но вы можете разработать собственное API бота.

С помощью этих механизмов можно расширять возможности своего бота, используя платформу VK Mini Apps. Например, можно реализовать настройки бота через приложение или сделать заполнение форм не через сообщения.

### Приложение «Выберите друга»

Приложение позволяет пользователю выбирать человека из списка друзей.

- [Добавьте приложение в сообщество](https://vk.com/add_community_app?aid=51625447).
- В сообществе включите событие «Событие от VK Mini Apps» в Callback или LongPoll API.
- Отправьте клавиатуру, используя клавишу `open_app` и указав идентификатор вашей группы в поле `owner_id`. В примере ниже используется поле `hash` для передачи параметров запуска приложения.

```json
{
    "inline": true,
    "buttons": [
        [{
            "action": {
                "type": "open_app",
                "label": "Выберите друга",
                "app_id": 6979558,
                "owner_id": -1234,
                "hash": "#entity=подарок&payload=randomuuid1234"
            }
        }]
    ]
}
```

- После выбора друга приложение отправит боту событие `app_payload`. Пример события:

```json
{
  "group_id": 1234,
  "type": "app_payload",
  "event_id": "166a12a0b4334b97f94c3fc33dfdd6d2c773e7b4",
  "v": "5.199",
  "object": {
    "user_id": 61351294,
    "app_id": 51625447,
    "payload": "
{\"from_id\":61351294,\"to_id\":355449586,\"payload\":\"randomuuid1234\"}"
  }
}
```

- В поле `to_id` будет указан ID выбранного друга.

### Приложение «Форма сбора заявок»

Приложение позволяет собирать заявки и делать опросы.

- [Добавьте приложение в сообщество](https://vk.com/add_community_app?aid=6013442).
- Создайте новую форму в настройках приложения.
- В сообществе включите событие «Lead forms: Новая заявка» в Callback или LongPoll API.
- Отправьте клавиатуру, используя клавишу `open_app` и указав id вашей группы в поле

`owner_id`.

```json
{
    "inline": true,
    "buttons": [
        [{
            "action": {
                "type": "open_app",
                "label": "Заполнить форму",
                "app_id": 6013442,
                "owner_id": -1234,
                "hash": "#form_id=1"
            }
        }]
    ]
}
```

- После отправки формы приложение отправит боту событие `lead_forms_new`. Пример события:

```json
{
  "group_id": 1234,
  "type": "lead_forms_new",
  "event_id": "b348eda8432391d89d9032eeca1cabdce14cedc4",
  "v": "5.199",
  "object": {
    "lead_id": 54321,
    "group_id": 1234,
    "user_id": 61351294,
    "form_id": 1,
    "form_name": "Тестовая форма",
    "answers": [
      {
        "key": "first_name",
        "question": "Имя",
        "answer": "Евгений"
      },
      {
        "key": "last_name",
        "question": "Фамилия",
        "answer": "Котляров"
      },
      {
        "key": "custom_1",
        "question": "Ваша любимая еда",
        "answer": "Оптоволокно"
      }
    ]
  }
}
```

## Кнопка «Начать»

Если кнопка включена и у пользователя ещё нет переписки с сообществом, мы показываем кнопку «Начать», которая отправляет команду `start`. Параметр `payload` этого сообщения будет выглядеть так:

```
{"command":"start"}
```
