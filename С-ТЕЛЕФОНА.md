# Как запустить с телефона

Ничего скачивать и распаковывать не нужно. Всё делается в браузере телефона
за три захода: подключить сборку → получить адрес → вписать его во ВКонтакте.

Понадобится: аккаунт GitHub (у вас уже есть) и 15 минут.

---

## Какую площадку выбрать

Главное требование — **адрес должен открываться из России без VPN**.
Приложение живёт внутри ВКонтакте, и если браузер пользователя не достучится
до хостинга, человек увидит пустую рамку. Проверять надо не свой доступ,
а доступ обычного пользователя.

| Площадка | Открывается в РФ | Что спросит |
|---|---|---|
| **GitHub Pages** | **да**, `github.io` работает | ничего, но нужен публичный репозиторий |
| **Российский хостинг** | **да**, надёжнее всего | от 150 ₽/мес, карта |
| **Netlify** | обычно да | вход через GitHub |
| Cloudflare Workers | **нет**, `workers.dev` заблокирован | почту |
| Vercel | **нестабильно** | иногда телефон |

> **Уже развернули на Cloudflare и открывается только через VPN?**
> Так и будет: домен `*.workers.dev` в России заблокирован целиком.
> Разворачивайте заново — варианты ниже. Свой домен, привязанный
> к Cloudflare, эту блокировку обходит, но это отдельные 200–300 ₽ в год.

Настраивать вручную ничего не нужно: в корне репозитория лежат `package.json`,
`netlify.toml` и `vercel.json` — площадка возьмёт команду сборки и папку
с результатом оттуда. Единственное, что стоит проверить, — **ветка**:
работа лежит в `claude/vk-mini-apps-knowledge-base-kos6be`, а площадки
по умолчанию берут `main`.

> **Vercel пишет «The verification phone number has been temporarily
> blocked»?** Это их защита от накруток, к приложению отношения не имеет.
> Ждать 12 часов не нужно — берите Cloudflare Pages или Netlify ниже,
> результат тот же.

---

## Заход 1, вариант 0 · GitHub Pages (бесплатно, открывается в РФ)

Самый быстрый рабочий вариант: хостинг прямо в репозитории, отдельная
регистрация не нужна. Условие одно — репозиторий должен быть публичным
(на бесплатном тарифе GitHub). Секретов в нём нет: ключи доступа живут
только в `.env`, который в репозиторий не попадает.

1. Откройте репозиторий на телефоне → **Settings** → пролистайте вниз
   до **Danger Zone** → **Change repository visibility** → **Public**
2. Там же в **Settings** → **Pages** → **Source** → выберите
   **GitHub Actions**
3. Вкладка **Actions** → слева **«Публикация на GitHub Pages»** →
   **Run workflow** → ветка `main` → **Run**

Через 2–3 минуты приложение будет по адресу:

```
https://zhenyagen.github.io/miniapps/
```

Дальше — заход 3, вписать этот адрес во ВКонтакте.

---

## Заход 1, вариант А · Cloudflare (телефон не нужен, но в РФ не открывается)

Cloudflare теперь заводит проекты как **Workers**, даже для обычных сайтов —
на экране будет написано «Configure your Worker project». Это нормально:
Worker умеет просто раздавать файлы, серверного кода у нас нет.

### Если вы на экране «Set up your application»

Заполните так:

| Поле | Значение |
|---|---|
| **Project name** | `2miniapps` |
| **Build command** | `npm run build` |
| **Deploy command** | `npx wrangler deploy` (уже стоит) |

Имя проекта должно совпадать с полем `name` в файле `wrangler.jsonc`
(сейчас там `2miniapps`) — иначе Cloudflare предупредит о расхождении
и заведёт лишний pull request. Адрес получится вида
`https://2miniapps.<ваш-аккаунт>.workers.dev`.

Раскройте **Advanced settings** и проверьте **ветку**: должна быть
`claude/vk-mini-apps-knowledge-base-kos6be`, а не `main`.

Дальше — **Deploy**. Первая сборка занимает 2–3 минуты.

### Если предлагают старый вариант — Pages

1. Откройте **[dash.cloudflare.com/sign-up](https://dash.cloudflare.com/sign-up)**,
   зарегистрируйтесь по почте и подтвердите её
2. Слева **Workers & Pages** → **Create** → вкладка **Pages** →
   **Connect to Git**
3. Разрешите доступ к GitHub, выберите репозиторий **miniapps**
4. Заполните три поля:

   | Поле | Значение |
   |---|---|
   | Production branch | `claude/vk-mini-apps-knowledge-base-kos6be` |
   | Build command | `npm run build` |
   | Build output directory | `dist` |

5. **Root directory не трогайте** — оставьте пустым. Сборка настроена так,
   чтобы работать из корня репозитория.
6. **Save and Deploy**

Через пару минут получите адрес вида `https://miniapps-xyz.pages.dev`.

---

## Заход 1, вариант Б · Netlify (вход через GitHub)

1. Откройте **[app.netlify.com](https://app.netlify.com)** →
   **Sign up** → **GitHub**
2. **Add new site** → **Import an existing project** → **GitHub** →
   репозиторий **miniapps**
3. **Branch to deploy** → `claude/vk-mini-apps-knowledge-base-kos6be`
4. Остальное менять не нужно: в корне репозитория лежит `netlify.toml`,
   из него Netlify возьмёт и папку сборки, и команду
5. **Deploy**

Адрес будет вида `https://имя-сайта.netlify.app`.

---

## Заход 1, вариант В · Vercel

Vercel сам возьмёт код из репозитория, соберёт его и выдаст адрес.
И будет пересобирать сам при каждом изменении.

1. Откройте в браузере телефона **[vercel.com](https://vercel.com)**
2. **Sign Up** → **Continue with GitHub** → разрешите доступ
3. **Add New…** → **Project**
4. В списке репозиториев найдите **miniapps** → **Import**

   > Если репозитория нет в списке — нажмите **Adjust GitHub App Permissions**
   > и дайте доступ к нему.

5. Откроется экран настроек. Нужно поменять **одно** поле:

   | Поле | Значение |
   |---|---|
   | **Production Branch** | `claude/vk-mini-apps-knowledge-base-kos6be` |

   Root Directory менять **не нужно** — оставьте корень. Команду сборки
   и папку с результатом Vercel возьмёт из `vercel.json`.

6. **Deploy** и подождите 1–2 минуты.

Когда закончится, Vercel покажет адрес вида:

```
https://miniapps-xxxx.vercel.app
```

Нажмите на него — должен открыться экран «Аудит страницы ВК».
Проверьте кнопкой **«Открыть демо-отчёт»**: если отчёт показался — сборка
рабочая.

> Кнопка «Войти» на этом адресе работать не будет — это нормально.
> Вход возможен, только когда приложение открывает сам ВКонтакте (заход 3).

### Если Vercel собрал не ту ветку

Работа лежит в ветке `claude/vk-mini-apps-knowledge-base-kos6be`, а Vercel
по умолчанию берёт `main`. Если после сборки открывается пустая страница
или старая версия:

**Settings** → **Git** → **Production Branch** → вписать
`claude/vk-mini-apps-knowledge-base-kos6be` → **Save** →
вкладка **Deployments** → у верхней сборки меню **⋯** → **Redeploy**.

---

## Заход 2. Скопировать адрес

На странице проекта адрес показан крупно сверху — у любой из трёх площадок.
Нажмите на него и скопируйте целиком, вместе с `https://`.

Должно получиться что-то вроде:

```
https://miniapps-abc123.vercel.app
```

---

## Заход 3. Вписать адрес во ВКонтакте (5 минут)

1. Откройте в браузере телефона **[dev.vk.ru](https://dev.vk.ru/)**
2. Войдите своей страницей ВК
3. **Мои приложения** → приложение **54693601**
4. **Настройки** и заполните три поля:

   | Поле | Что вписать |
   |---|---|
   | Адрес приложения | адрес из захода 2 |
   | Адрес для мобильных | тот же адрес |
   | Доверенный redirect URI | `http://localhost:8910/callback` |

5. **Сохранить**

> Третье поле нужно не мини-приложению, а десктопной версии — чтобы она
> работала на этом же приложении ВК. Впишите сразу, чтобы потом не возвращаться.

---

## Готово. Открыть

В браузере телефона или в приложении ВКонтакте:

```
https://vk.com/app54693601
```

Нажмите **«Войти»** → ВК спросит разрешение на доступ к сообществам
и статистике → появится список ваших сообществ.

Нажимаете сообщество — через полминуты готов отчёт: сводка, зоны роста,
план на 4 недели, контент и конкуренты.

---

## Что делать, если

**«Приложение недоступно» или пустая рамка в ВК.**
Адрес в настройках не совпадает с тем, что выдал Vercel. Проверьте, что
скопировали целиком, с `https://` и без пробела в конце.

**Открывается страница «Файл открыт напрямую с диска».**
Это сообщение появляется, только если открыть файл с диска. Значит в поле
адреса попало что-то не то — вернитесь к заходу 2.

**Кнопка «Войти» ничего не делает.**
Вы открыли приложение по адресу Vercel, а не через `vk.com/app54693601`.
Вход работает только внутри ВКонтакте.

**Vercel пишет `Error: No Output Directory named "dist"`.**
Root Directory выставлен в `apps/vk-audit` — верните корень:
**Settings** → **General** → **Root Directory** → очистить → **Redeploy**.

**Vercel требует подтверждение по телефону, номер заблокирован.**
Возьмите Cloudflare Pages (вариант А) — там регистрация по почте.
Приложение и настройки те же, разницы в результате нет.

**Cloudflare пишет `Failed to match Worker name`.**
Имя проекта в Cloudflare и поле `name` в `wrangler.jsonc` разошлись.
Деплой всё равно проходит — Cloudflare берёт своё имя. Чтобы предупреждение
ушло, приведите их к одному значению.

**Cloudflare пишет `Missing entry-point to Worker script` или подобное.**
Не заполнено поле **Build command** — Worker пытается деплоить пустоту.
Впишите `npm run build`, а Deploy command оставьте `npx wrangler deploy`.
Файл `wrangler.jsonc` в репозитории уже говорит, что раздавать нужно
папку `dist`.

**Сборка падает с `Could not read package.json` в `/opt/buildhome/repo`.**
Это было до правки от 2 августа: сборка искала файлы в подпапке. Сейчас
всё собирается из корня. Нажмите **Retry deployment** — или, если проект
создавали раньше, проверьте в настройках сборки: **Build command**
`npm run build`, **Output directory** `dist`, **Root directory** пустое.

**Сборка падает на тестах.**
Так и задумано: тесты сверяют расчёт с десктопной версией, и упавший тест
означает, что цифры разошлись. Напишите мне — разберусь.

**Адрес открывается только через VPN.**
Домен хостинга заблокирован в России — так со всеми `*.workers.dev`
и периодически с `*.vercel.app`. Для мини-приложения это приговор: люди
увидят пустую рамку. Переезжайте на GitHub Pages (вариант 0) или на
российский хостинг.

**Хочу обновить приложение после изменений.**
Ничего делать не нужно: площадка пересоберёт сама при каждом изменении
в ветке. Адрес останется прежним.

**Совсем ничего не выходит, нужен адрес прямо сейчас.**
Скачайте архив `vk-audit-app.zip` из переписки, откройте
[app.netlify.com/drop](https://app.netlify.com/drop) и загрузите **сам архив**
(кнопка выбора файла работает и на телефоне). Регистрация не нужна вовсе,
адрес выдаётся сразу. Минус один: при следующих правках архив придётся
загружать заново.

---

## Дальше, когда будет компьютер

- **Свой домен** вместо `miniapps-abc123.vercel.app` — 200–300 ₽ в год,
  привязывается в настройках Vercel: [ИНСТРУКЦИЯ.md](ИНСТРУКЦИЯ.md#сколько-стоит-и-где-это-брать)
- **Бот с разборами в личку** — нужен сервер:
  [ИНСТРУКЦИЯ.md, часть 2](ИНСТРУКЦИЯ.md#часть-2--бот-с-разборами)
