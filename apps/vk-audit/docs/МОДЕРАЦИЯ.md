# Модерация: что требует платформа и как это сделано

Заметки по отказам и правкам под них. Правила — [dev.vk.ru/ru/mini-apps-rules](https://dev.vk.ru/ru/mini-apps-rules),
редакция от 17 марта 2026.

---

## Отказ от 4 августа 2026 · пункт 1.2.2

Формулировка модератора:

> В приложениях платформы VK Mini Apps важно реализовать бесшовную
> авторизацию пользователя по `vk_user_id` в соответствии с п. 1.2.2.
> Авторизация по почте, телефону или через системы авторизации,
> например VK ID, избыточны.

Сам пункт:

> 1.2.2. Приложение, не являющееся Игрой, должно авторизовывать
> Пользователей по параметрам запуска и валидировать подпись параметров
> запуска в соответствии с документацией.

### Что было не так

Первый экран показывал блок «Доступ к данным» с заголовком «Нужен вход
через ВКонтакте» и кнопкой «Войти». Технически это был обычный
`VKWebAppGetAuthToken`, но выглядело и работало как отдельный вход:
пока человек не нажмёт кнопку, приложение им не занималось.

Внутри ВКонтакте это лишнее по определению — платформа передаёт
`vk_user_id` в параметрах запуска, то есть человек уже опознан.

### Что сделано

**Экран входа убран изнутри ВК совсем.** Человек сразу видит поле,
выбор периода и кнопку «Проверить мою страницу» — она берёт `vk_user_id`
из параметров запуска и разбирает его страницу без единого вопроса.

**Ключ доступа запрашивается в момент сбора, а не до него.** Он нужен
только чтобы прочитать записи и статистику; окно прав ВКонтакте
показывает тогда, когда человек нажал «собрать» — и в этот момент
понятно, зачем права. Это же требует пункт 1.1.2: согласие спрашивается
под уже внедрённую функцию.

**Отказ обрабатывается и не ломает приложение** (пункт 1.1.9): если
человек не дал доступ, появляется объяснение, зачем он нужен, и сбор
можно повторить. Демо-режим при этом работает всегда — он вообще
не обращается к ВКонтакте.

Экран входа остался **только вне ВКонтакте**, где параметров запуска
не существует: приложение открывается и на GitHub Pages напрямую,
и локально с возвратом на `localhost:8910`.

### Что осталось нерешённым

Вторая половина пункта 1.2.2 — **валидация подписи параметров запуска**.
Подпись проверяется секретом приложения, а секрет нельзя класть
в браузерный бандл: он распаковывается (это же запрещает пункт 1.2.5).
Значит, для проверки нужен серверный обработчик.

Сейчас его нет и проверять нечего: приложение статическое, своего
бэкенда у него не существует, данные никуда не уходят и никакие решения
на основании `vk_user_id` не принимаются — он используется только как
подсказка «чью страницу открыть по кнопке». Подделка параметров даёт
ровно ту же возможность, что и ручной ввод чужой ссылки, которая
и так открыта всем.

Если модерация вернётся с этим требованием, вариантов два:

1. поднять маленький серверный обработчик, который проверяет подпись
   и отдаёт приложению «да/нет» — это ломает нынешнюю схему «без
   сервера вовсе»;
2. объяснить в переписке, что валидировать нечего: серверной стороны
   нет, доверенных действий по параметрам запуска не совершается.

Второй ответ честный, но принимает его не всегда — закладывайтесь
на первый.

---

## Отказ от 5 августа 2026 · раздел 5.4

Формулировка модератора:

> Сервис предлагает купить цифровые ценности. Монетизация цифровых
> ценностей регулируется разделом 5.4. По п. 5.4.1 оплата разрешается
> только в версиях vk.ru и m.vk.ru. На мобильных клиентах iOS и Android
> не должно быть оплаты, уводов или подсказок, как и где можно оплатить,
> даже в текстовом виде.

### Что такое «цифровые ценности»

Глоссарий правил:

> Цифровые ценности — услуги, права на результаты интеллектуальной
> деятельности, предоставляемые и потребляемые в электронной форме.

То есть под определение попадает и донат за приложение, и подписка
на разборы, и услуги по упаковке сообщества. Всё это в приложении
продавать нельзя.

### Что было не так

Три места:

1. **Блок «Поддержать»** с кнопкой на `vk.ru/…?w=donut_payment-…` —
   прямой увод на окно оплаты.
2. **Блок «Не хотите делать сами?»** — перечень услуг за деньги:
   упаковка сообщества, обложка, макеты, контент-план.
3. **Карточка подписки на бота** — упоминание платных уровней VK Donut.

Формально пункт 5.4.1 разрешает оплату на `desktop_web` и `mobile_web`,
и можно было бы прятать блоки по `vk_platform`. Но тогда включается
пункт 5.4.2: приложение целиком должно соответствовать правилам
приложений-магазинов — реквизиты продавца, оферта, условия возврата.
Ради кнопки доната это несоразмерно.

### Что сделано

**Из приложения убраны все упоминания денег.** Ни доната, ни услуг,
ни платных уровней подписки — ни ссылкой, ни текстом. Вместо блока
услуг остался один блок «Вопросы по отчёту» с личными сообщениями
автору: связь — социальная механика платформы, а не продажа.

**Деньги живут вне приложения.** VK Донат — на странице сообщества,
услуги — в закрепе и меню сообщества. Туда человек приходит сам,
и правила мини-приложений на это не распространяются.

**Подписка на бота остаётся бесплатной функцией.** Платные уровни,
если появятся, описываются в сообществе бота, а не в приложении.

---

## Отказ от 6 августа 2026 · реклама

Формулировка модератора:

> Если мы правильно вас поняли, вы планируете монетизироваться через
> рекламу? В таком случае её нужно внедрить уже сейчас, чтобы мы могли
> всё проверить. Для заработка важно зарегистрировать кабинет выплат
> на СЗ, ИП или ЮЛ, а также обязательно выбрать исполнителя.

Логика такая: продавать что-либо внутри приложения нельзя (раздел 5.4),
значит, единственная разрешённая монетизация — реклама рекламной сети
ВКонтакте. И проверять её модерация хочет в работающем виде, а не по
обещанию.

### Что сделано

**Баннер внизу экрана** — `VKWebAppShowBannerAd` с `layout_type: 'resize'`.
Именно `resize`, а не `overlay`: баннер отжимает содержимое вверх,
а не накрывает его. С `overlay` он закрыл бы подвал со ссылкой
на политику конфиденциальности, а это отдельное нарушение.

**Реклама между экранами** — один раз за сеанс, на стыке «собрали» →
«показали отчёт». Это единственное место, где человек и так ждёт.
Материалы просятся заранее (`VKWebAppCheckNativeAds` при запуске),
иначе показ упрётся в загрузку ровно тогда, когда отчёт уже готов.

**Отказ обрабатывается молча.** Вне ВКонтакте моста нет, внутри
рекламных материалов может не оказаться — ни то ни другое не должно
ломать отчёт. Если человек закрыл баннер кнопкой, в этом сеансе он
больше не поднимается.

**Политика конфиденциальности обновлена.** Раньше там было сказано
«не использует cookies и рекламные трекеры» — с рекламной сетью это
перестало быть правдой. Теперь отдельный раздел: что показывается,
кто показывает и что приложение рекламной сети не передаёт ничего.

### Что нужно со стороны кабинета

Регистрация в кабинете выплат на самозанятого и выбор исполнителя —
это делается в личном кабинете dev.vk.ru, кодом не решается. Пока
приложение не прошло проверку, показы идут в тестовом режиме
и не учитываются.

---

## Отказ от 2 сентября 2026 · название и UX брифа

Две претензии сразу.

### Название

> Согласно п. 3.6.4 просим убрать «ВК» из названия приложения.

Модератор ошибся номером: про бренд говорит пункт **3.6.2** —
«запрещается использовать в названиях и описаниях бренд „VK“, „ВК“ или
„ВКонтакте“». Пункт 3.6.4 про обложки и иконки. Требование от этого
не перестаёт быть законным.

«Аудит страницы ВК» → **«Аудит страницы»**. Название вынесено
константой `APP_NAME` в `src/config.ts` и подхватывается везде, кроме
карточки на dev.vk.ru — её правит человек руками. Из подзаголовка
на первом экране «ВКонтакте» тоже убрано: это описание.

Тот же пункт требует, чтобы название давало «минимальную информацию
о содержании», а 3.6.3 — чтобы не совпадало с другим приложением
каталога. Если «Аудит страницы» окажется занято, менять здесь же.

### UX брифа

> Как мы понимаем, ваша идея в том, что пользователь должен скопировать
> бриф, вставить его в дипсик и получить какой-то результат? Здесь было
> бы хорошо поработать над UX, потому что сейчас это плохо считывается.
> Например, добавить больше информации о возможных действиях пользователя
> с брифом. Также можно добавить онбординг и faq.

Претензия справедливая. Раньше внизу отчёта стояли две кнопки и абзац
мелким шрифтом: человек, впервые видящий слово «бриф», не понимал ни
что скопируется, ни куда это вставлять, ни что вернётся.

**Карточка «Разобрать отчёт нейросетью»** (`components/BriefCard.tsx`) —
три пронумерованных шага вместо двух кнопок. Кнопка первого шага после
нажатия меняется на «Скопировано — шаг 2», кнопка второго становится
основной: видно, где ты в последовательности. Плюс два раскрывающихся
блока — «Что именно скопируется» (семь разделов) и «Что вернёт
нейросеть» (пример ответа на вымышленных данных).

**Онбординг** — блок «Как это работает» над формой на первом экране:
три шага, ссылка → отчёт → бриф. Не всплывающий тур: хранилища
у приложения нет, а блок над формой не мешает тем, кто уже знает,
что делать.

**FAQ** — третья вкладка первого экрана (`components/FaqView.tsx`):
восемь вопросов, которые задают до первого запуска. Что получу, сколько
ждать, нужно ли быть администратором, куда уходят данные, откуда берутся
выводы, платно ли.

**Шаг «Нейросеть» в инструкции** — со скриншотом карточки: отдельная
претензия из отказа 19 августа, «в разделе „Инструкция“ ничего не сказано
про DeepSeek».

---

## Что проверить перед повторной подачей

- политика конфиденциальности открывается по прямой ссылке
  и описывает то, что происходит на самом деле;
- демо-режим работает без единого запроса к ВКонтакте;
- при отказе в правах приложение объясняет причину и даёт повторить;
- скриншоты в карточке сняты с текущей версии — на старых был экран
  входа, из-за которого и пришёл отказ;
- в приложении нет ни одной ссылки и ни одной строки про оплату,
  донаты, услуги и платные подписки — проверять поиском по `донат`,
  `оплат`, `поддержать`, `подписк`, `Donut`;
- реклама показывается и не перекрывает интерфейс: баннер в режиме
  `resize`, подвал с политикой виден;
- кабинет выплат зарегистрирован и исполнитель выбран — без этого
  показы идут в тестовом режиме;
- ни в названии, ни в описаниях нет «ВК», «VK» и «ВКонтакте» —
  проверять поиском по `APP_NAME` и по текстам первого экрана.
