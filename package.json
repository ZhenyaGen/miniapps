{
  "name": "miniapps",
  "private": true,
  "version": "1.0.0",
  "description": "Мини-приложения ВКонтакте: аудит страницы и бот с разборами",
  "scripts": {
    "build": "npm --prefix apps/vk-audit ci && npm --prefix apps/vk-audit run build && node scripts/collect-dist.mjs",
    "test": "npm --prefix apps/vk-audit test && npm --prefix apps/vk-bot test",
    "dev": "npm --prefix apps/vk-audit run dev",
    "smoke": "npm --prefix apps/vk-audit run build && node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "lottie-web": "^5.13.0",
    "playwright": "^1.62.1",
    "wrangler": "^4.118.0"
  }
}
